name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  node:
    uses: nubitio/.github/.github/workflows/reusable-ci-node.yml@main
    with:
      node-version: "22"
      package-manager: "pnpm"
